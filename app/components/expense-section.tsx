import { View } from "react-native";
import { Text } from "react-native-paper";
import React from "react";

const ExpenseSection = () => {
    return (
        <View>
            <Text>ExpenseSection</Text>
        </View>
    );
};

export default ExpenseSection;
